sudo systemctl start docker

sudo systemctl stop docker

sudo systemctl restart docker

sudo systemctl status docker

sudo yum install epel-release
sudo yum install htop

//install git on ec2

sudo yum install git -y

git clone https://github.com/lakshmikant-lpu/aws_services.git

cd aws_services

git checkout lesson22_docker

cd lesson22_docker/nodejs-crud-api

git pull origin lesson22_docker

sudo docker build -t nodejs-crud-api .

sudo docker images

docker run -p 3000:3000 nodejs-crud-api

sudo docker run -d -p 3000:3000 nodejs-crud-api // run in background

sudo docker ps

curl http://localhost:3000

curl <ec2>:3000/users

give access to inbount for 3000 port to access from outside cloud vpc

E,g http://ec2-35-171-87-132.compute-1.amazonaws.com:3000/users

// This is for local

curl -sL https://rpm.nodesource.com/setup_16.x | sudo -E bash -
sudo yum install -y nodejs

node -v
npm -v

curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.3/install.sh | bash

source ~/.bashrc 

nvm -v

sudo npm install -g pm2